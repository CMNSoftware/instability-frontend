import React from 'react'
import { Text, SafeAreaView } from 'react-native'

export default function Router() {
    return (
        <SafeAreaView>
            <Text>Router</Text>
        </SafeAreaView>
    )
}
